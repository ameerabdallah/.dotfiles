# .dotfiles

### Use

* Clone Repo
* Install:
    ```bash
    ./install
    ```
* Pull and Install:
    ```bash
    ./install -u ## or --pull
    ```

### Software that gets installed
* Rustup toolchain (via curl)
* nvm (via curl)
* thefuck (via pip)
